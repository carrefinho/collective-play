let x, y;

function setup() {
    createCanvas(windowWidth, winodwHeight);

}

function draw() {
    r = map(rotationX, -90, 90, 0, 255);
    g = map(rotationX, -90, 90, 0, 255);
    b = map(rotationX, -90, 90, 0, 255);
    background(rgb())
}
